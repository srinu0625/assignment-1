
# Bullish Trend
if sma_t4 < sma_t3 < sma_t2 < sma_t1 and position == 0:  # Bullish trend
    entry_price = df['close of sma_t1'].iloc[i]  # Close price at SMA_t1
    position = 1  # Enter long position

elif df['close of sma_t1'].iloc[i] < df['close of sma_t2'].iloc[i] and position == 1:  # Bearish reversal (exit long)
    exit_price = df['close of sma_t2'].iloc[i]  # Close price at SMA_t2
    position = 0  # Exit long position

# Bearish Trend
elif sma_t4 > sma_t3 > sma_t2 > sma_t1 and position == 0:  # Bearish trend
    entry_price = df['close of sma_t1'].iloc[i]  # Close price at SMA_t1
    position = -1  # Enter short position

elif df['close of sma_t4'].iloc[i] > df['close of sma_t3'].iloc[i] and position == -1:  # Bullish reversal (exit short)
    exit_price = df['close of sma_t2'].iloc[i]  # Close price at SMA_t2
    position = 0  # Exit short position
